/**
 * generateSignature
 * @description Makes a request to an endpoint to sign Cloudinary parameters as part of widget creation
 */
interface GenerateSignatureCallback {
    fetch: Function;
    signatureEndpoint: string;
}
declare function generateSignatureCallback({ signatureEndpoint, fetch: fetcher, }: GenerateSignatureCallback): (callback: (signature: string | null, error?: unknown) => void, paramsToSign: object) => void;

export { type GenerateSignatureCallback, generateSignatureCallback };
