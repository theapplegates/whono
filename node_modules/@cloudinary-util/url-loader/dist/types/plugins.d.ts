import { AspectRatio, CropMode, Gravity, Height, Format, X, Y, Width, Zoom } from '../constants/parameters.js';
import '../lib/utils.js';

interface PluginOptions {
    aspectRatio?: AspectRatio;
    crop?: CropMode;
    gravity?: Gravity;
    height?: Height;
    format?: Format;
    resize?: string;
    x?: X;
    y?: Y;
    width?: Width;
    zoom?: Zoom;
}
interface PluginResults {
    options?: PluginOptions;
}

export type { PluginOptions, PluginResults };
