/**
 * constructTransformation
 * @description Constructs a transformation string to append to a URL
 * @param {object} settings: Configuration including prefix, qualifier, and value
 */
interface ConstructTransformationSettings {
    prefix?: string;
    qualifier?: string | boolean;
    value?: string | number | boolean;
    converters?: ReadonlyArray<QualifierConverters>;
}
declare function constructTransformation({ prefix, qualifier, value, converters, }: ConstructTransformationSettings): string | undefined;
/**
 * promptArrayToString
 */
declare function promptArrayToString(promptArray: ReadonlyArray<string>): string;

interface QualifierConverters {
    convert: (value: any) => ConstructTransformationSettings["value"];
    test: (value: any) => boolean;
}
interface QualifierConfig {
    location?: string;
    order?: number;
    prefix?: string;
    qualifier?: string | boolean;
    converters?: ReadonlyArray<QualifierConverters>;
}

export { type ConstructTransformationSettings as C, type QualifierConfig as Q, type QualifierConverters as a, constructTransformation as c, promptArrayToString as p };
